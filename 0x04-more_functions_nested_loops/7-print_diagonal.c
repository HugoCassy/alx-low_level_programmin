#include "main.h"
/**
 * print_diagonal - prints the diagonal
 * @n: valuse to print
 * Return: 0
 */
void print_diagonal(int n)
{
	int b;

	if (n > 0)
	{
		for (b = 0; b < n; b++)
		{
			_p
			_putchar (98);
		}
		_putchar ('\n');
	}
	else
	{
		_putchar ('\n');
	}
}
