#include "main.h"
/**
 * _strlen - get the length of a string
 * @s: pointer to the memory of a string
 *
 * Return: c
 */
int _strlen(char *s)
{
	int len;

	while (s[length] != '\0')
	{
		len++;
	}
	return (len);
}
